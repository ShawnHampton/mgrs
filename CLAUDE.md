# Project Rules

- Always use `pnpm` as the package manager (not npm or yarn).
- Always use package.json scripts (e.g. `pnpm build`, `pnpm test`) instead of invoking tools directly (e.g. `npx tsc`, `npx vite build`).
- Do not start the dev server. The developer will handle running it.
